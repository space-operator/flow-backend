[package]
name = "rhai-script"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
flow-value = { version = "0.1.0", path = "../../lib/flow-value", default-features = false }
rhai = { version = "1.16.3", features = ["decimal"] }
